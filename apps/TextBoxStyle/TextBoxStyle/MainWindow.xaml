<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TextBoxStyle.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TextBoxStyle"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TextBoxStyle">

    <!--<StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" 
                Background="DarkSlateGray"
                Spacing="5">
        <StackPanel.Resources>
            <Style x:Key="Custom" BasedOn="{StaticResource DefaultTextBoxStyle}" TargetType="TextBox">
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Visible" />
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Visible" />
            </Style>
        </StackPanel.Resources>
        <TextBox Text="Hello" IsReadOnly="True"/>
        <TextBox Width="300" HorizontalAlignment="Right" Text="Yellow" IsEnabled="False"/>
        <TextBox Height="50" HorizontalAlignment="Center"/>
        <TextBox HorizontalContentAlignment="Right" Height="100" 
                 Background="Orange" BorderBrush="Red"
                 ScrollViewer.HorizontalScrollBarVisibility="Visible" 
                 BorderThickness="20"/>
        <TextBox VerticalContentAlignment="Bottom" Height="50" ScrollViewer.HorizontalScrollBarVisibility="Visible" />
        <TextBox Style="{StaticResource Custom}" Width="300" Margin="5"/>
        <TextBox Width="300" Margin="5" Acc
         ScrollViewer.HorizontalScrollBarVisibility="Visible"/>
        <TextBox TextWrapping="Wrap" Width="300" Height="60" ScrollViewer.VerticalScrollBarVisibility="Visible"/>
    </StackPanel>-->


    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
        <MenuBar>
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="New"/>
                <MenuFlyoutItem Text="Open..."/>
                <MenuFlyoutItem Text="Save"/>
                <MenuFlyoutItem Text="Exit"/>
                    <ToggleMenuFlyoutItem Text="Undo">
                        <ToggleMenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="Accept" />
                        </ToggleMenuFlyoutItem.Icon>
                    </ToggleMenuFlyoutItem>
                    <MenuFlyoutItem Text="Cut">
                        <MenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="Account"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <ToggleMenuFlyoutItem Text="Copy"/>
                    <MenuFlyoutItem Text="Paste"/>

            </MenuBarItem>

            <MenuBarItem Title="Edit">
                <ToggleMenuFlyoutItem Text="Undo">
                    <ToggleMenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Accept" Height="10" Width="10" />
                    </ToggleMenuFlyoutItem.Icon>
                </ToggleMenuFlyoutItem>
                <MenuFlyoutItem Text="Cut">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Account" Height="10" Width="10"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <ToggleMenuFlyoutItem Text="Copy"/>
                <MenuFlyoutItem Text="Paste"/>
            </MenuBarItem>

            <MenuBarItem Title="Help">
                <MenuFlyoutItem Text="About"/>
            </MenuBarItem>
        </MenuBar>

    </StackPanel>
</Window>

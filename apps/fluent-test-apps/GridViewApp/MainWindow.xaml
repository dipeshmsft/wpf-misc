<Window x:Class="GridViewApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GridViewApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Background="#202020">
    <StackPanel>
        <ListView ItemsSource="{Binding Cars}" Margin="5">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Make"
                            DisplayMemberBinding="{Binding Path=Make}" />
                    <GridViewColumn Header="Model"
                            DisplayMemberBinding="{Binding Path=Model}" />
                </GridView>
            </ListView.View>
        </ListView>

        <ListView ItemsSource="{Binding Cars}" Margin="5">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Make}" Margin="0 0 5 0"/>
                        <TextBlock Text="{Binding Model}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Window>
